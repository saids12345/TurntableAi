import type { BrainContext } from "@/lib/brain/brainContext";

export async function runPerceptionLayer(
  context: BrainContext,
): Promise<void> {
  // Perception is responsible for
  // understanding the current
  // state of the restaurant.

  void context;
}